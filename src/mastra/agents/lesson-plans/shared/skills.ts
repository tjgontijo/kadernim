export const RESOURCE_FIDELITY = `Fidelidade ao recurso:
- Use apenas dados do snapshot fornecido.
- useResourceStepIds aceita somente ids presentes em snapshot.steps.
- Sem id aplicavel -> useResourceStepIds: [].
- BNCC: copie codigos do snapshot; nunca invente.
- Objetivos, serie, disciplina e etapa vem do snapshot.`

export const OUTPUT_CONTRACT = `Contrato de saida:
- JSON valido no schema fornecido.
- Sem markdown, sem texto fora do JSON, sem campos extras.
- Conteudo em pt-BR, direto e pratico para o professor.`
